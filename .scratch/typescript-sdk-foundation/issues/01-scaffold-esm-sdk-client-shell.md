Status: completed

# Scaffold ESM SDK With Runtime-Neutral Client Shell

## Parent

.scratch/typescript-sdk-foundation/PRD.md

## What to build

Create the initial TypeScript SDK package foundation with ESM-only packaging, runtime-neutral root exports, a runtime-specific helper export, a `TwikitClient` shell, optional adapter injection, service namespaces, and initial documentation/examples that preserve the public API sketches.

## Acceptance criteria

- [x] The package builds as ESM-only and exposes a runtime-neutral root entrypoint plus a runtime-specific helper entrypoint.
- [x] The package is named `twikit-sdk`, uses the same license as Python twikit, and includes README attribution to `d60/twikit` without copying Python source.
- [x] `TwikitClient` can be constructed with language, user agent, session, and optional `fetch` adapter options.
- [x] Service namespaces exist for session, users, tweets, search, media, and future auth without implementing network behavior yet.
- [x] The public API examples from `.scratch/typescript-sdk-foundation/API-SKETCH.md` are represented in docs or tests as compile-time contracts where practical.
- [x] Vitest-based unit/build/typecheck commands exist and can run without Python, live X credentials, browser automation, Bun-specific test paths, or network access.
- [x] The NPM published package only hosts the built and bundled code.

## Blocked by

None - can start immediately

## Comments

> *This was generated by AI during triage.*

Implemented the initial ESM-only TypeScript SDK scaffold:

- Added root and `./node` package exports backed by TypeScript build output.
- Added `TwikitClient`, runtime-neutral `MemorySession`, Node-only `FileSession`, service namespaces, DTO/page contracts, and `TwikitError`.
- Added README attribution to `d60/twikit`, MIT license, build/typecheck/test scripts, and Vitest compile-time API contract tests.
- Verified with `pnpm typecheck`, `pnpm build`, and `pnpm test`.

> *This was generated by AI during triage.*

TDD verification pass completed:

- Added a built-package export contract test covering `dist/index.js` and `dist/node.js`.
- Updated `pnpm test` to build before running Vitest so the export contract works from a fresh checkout.
- Re-verified with `pnpm typecheck`, `pnpm build`, `pnpm test`, and `pnpm pack --dry-run`.

> *This was generated by AI during triage.*

Closed for release `twikit-sdk@0.0.2`.
